plugins {
    id 'application'
    id "org.openjfx.javafxplugin" version "0.0.14"
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

javafx {
    version = "20"
    modules = [ 'javafx.controls' ]
}

mainClassName = 'com.yudit.MicrocodeSheetApplication'